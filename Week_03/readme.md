## 排版与渲染
1. 预处理
     抽象一个对象，保存方位信息
2. 收集元素进行
  - 根据主轴尺寸，把元素分进行里面
  - 若设置了 no-wrap，则强行分配进第一行
3. 计算主轴
   - 找出所有 Flex 元素
   - 把主轴方向的剩余尺寸按比例分配给这些元素
   - 把剩余空间为负数，所有 flex 元素为 0，等比例压缩剩余元素
4. 计算交叉轴
   - 根据每一行中最大元素尺寸计算行高
   - 根据行高 flex-align 和 item-align，确定元素具体位置
